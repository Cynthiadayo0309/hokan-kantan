# AGENTS.md

## 1. このファイルの目的

このリポジトリでは、Codexは本ファイルを最優先の開発指示として扱うこと。

要件にない機能を独自判断で追加せず、ITに不慣れな利用者が迷わず使える、シンプルで保守しやすいWindows向けデスクトップアプリを実装すること。

---

## 2. アプリ基本情報

- アプリ表示名：**訪看かんたん計算**
- サブタイトル：**施設内訪問看護 月額費用シミュレーター**
- プロジェクト名：`hokan-kantan`
- Windows実行ファイル名：`訪看かんたん計算.exe`
- インストーラー名：`訪看かんたん計算_Setup_<version>.exe`

---

## 3. アプリの目的

施設内で医療保険の訪問看護を利用する1人の利用者について、1か月分の訪問予定と算定条件を入力し、次の概算額を算出する。

- 訪問看護にかかる月額費用総額
- 自己負担割合を設定した場合の利用者負担額

本アプリは概算ツールであり、請求処理、レセプト作成、正式な請求額の確定は行わない。

画面上に次の注意書きを常時または計算結果付近に表示すること。

> 本計算結果は概算です。実際の算定・請求内容を保証するものではありません。

---

## 4. 想定利用環境

- Windows 64bit PCで使用する
- 1台のPCを数名が交代で使用する
- 同時編集は想定しない
- 利用者はITリテラシーが高くない
- インターネット接続なしで使用できる
- 複数PC間でデータ共有しない
- ログイン、ユーザー登録、権限管理は実装しない
- 利用者にNode.js、npm、SQLite、開発ツールの導入やコマンド操作を求めない

---

## 5. 配布方式

利用者にはソースコードではなく、Windows用インストーラーを配布する。

利用者の操作は次の流れとする。

1. インストーラーを実行する
2. 案内に従ってインストールする
3. デスクトップアイコンから起動する
4. アプリを操作する

インストーラーは次を満たすこと。

- 利用者単位のインストールを基本とする
- デスクトップショートカットを作成する
- スタートメニューへ登録する
- アンインストーラーを作成する
- アップデート時に利用者データを保持する
- アンインストール時に利用者データを自動削除しない
- インストール完了後にアプリを起動できる

コード署名は初期版の対象外とし、未署名インストーラーではWindowsの警告が表示される可能性があることをREADMEへ記載する。

---

## 6. 技術スタック

### デスクトップアプリ

- Electron
- TypeScript
- electron-builder
- Windows用NSISインストーラー

### フロントエンド

- Vue.js 3
- Composition API
- `<script setup lang="ts">`
- Vite
- Vuetify
- Vue Router
- Piniaは画面間で共有する状態に限って使用する

### データベース

- SQLite
- `better-sqlite3`
- データベースアクセスはElectronメインプロセスで行う
- VueのレンダラープロセスからSQLiteを直接操作しない

### テスト

- Vitest
- Vue Test Utils
- 計算ロジック、時間帯判定、DB処理を単体テストする

### 使用しないもの

- Express
- 外部Webサーバー
- Nuxt
- SSR
- GraphQL
- Docker必須構成
- クラウドサービス
- 外部API
- ネットワーク上のSQLite
- マイクロサービス

---

## 7. Electronセキュリティ要件

次を必須とする。

- `contextIsolation: true`
- `nodeIntegration: false`
- `sandbox: true`を可能な範囲で使用する
- preloadスクリプトと`contextBridge`を使用する
- レンダラープロセスからNode.js APIを直接呼ばせない
- IPCでは必要最小限の機能だけ公開する
- IPCの入力値をメインプロセス側でも検証する
- SQL文をレンダラープロセスから受け取らない
- レンダラープロセスから任意のファイルパスを指定させない

公開APIの例：

```ts
window.hokanApi.getEstimate()
window.hokanApi.saveEstimate()
window.hokanApi.saveDailyVisit()
window.hokanApi.deleteDailyVisit()
window.hokanApi.calculateMonthlyEstimate()
window.hokanApi.resetEstimate()
window.hokanApi.getPricingVersion()
```

---

## 8. データ保存

SQLiteファイルはアプリのインストール先へ保存しない。

保存先にはElectronの`app.getPath("userData")`を使用する。

例：

```text
C:\Users\<ユーザー名>\AppData\Roaming\訪看かんたん計算\application.db
```

初回起動時に自動で次を行うこと。

1. 保存フォルダを作成する
2. SQLiteファイルを作成する
3. テーブルを作成する
4. DBマイグレーションを実行する
5. 料金マスターを登録する
6. 初期画面を表示する

DB構造を変更する場合は、起動時にマイグレーションを実行する。

マイグレーション前にDBファイルのバックアップを作成する。

アプリ更新時に既存DBを削除しない。

---

## 9. 画面構成

画面は次の2画面のみとする。

1. 月間入力画面
2. 費用明細画面

画面上部に現在の操作位置を表示する。

```text
1. 月間予定を入力
2. 費用を確認
```

以下の画面は作成しない。

- ログイン
- 利用者一覧
- 施設一覧
- 職員管理
- 設定管理
- 請求
- レセプト

---

# 10. 画面1：月間入力画面

## 10.1 目的

サービス提供票を参考に、1人の利用者について1か月分の訪問予定と算定条件を入力する。

参考画像がリポジトリに存在する場合は、日付の並び、月間の一覧性、サービス提供票らしい構成の参考にする。ただし、帳票の完全再現より操作性を優先する。

## 10.2 ヘッダー入力項目

| 項目 | 入力方式 | 必須 | 初期値 |
|---|---|---:|---|
| 利用者名 | テキスト | 必須 | 空欄 |
| 施設名 | テキスト | 任意 | 空欄 |
| 対象年月 | 年月選択 | 必須 | 当月 |
| 同一建物人数区分 | プルダウン | 必須 | 3人以上9人以下 |
| 自己負担割合 | プルダウン | 必須 | 未設定 |

### 同一建物人数区分

- 1人または2人
- 3人以上9人以下
- 10人以上19人以下
- 20人以上49人以下
- 50人以上

### 自己負担割合

- 未設定
- 1割
- 2割
- 3割

自己負担割合が未設定の場合、月額費用総額のみ表示し、利用者負担額は表示しない。

---

## 11. 月間入力エリア

- 横方向に対象月の1日から末日までを表示する
- 対象月に存在しない日付は表示しないか、無効表示とする
- 土曜日、日曜日、祝日を視覚的に区別する
- 今日が対象月に含まれる場合は強調する
- 横スクロール時も日付行と左端の項目を固定する
- 日付セルを1回クリックすると日別入力ダイアログを開く
- ダブルクリックを必須にしない
- 訪問のない日はデータを登録しない
- 「訪問あり／なし」の項目は作成しない
- 日付にデータが登録されていることをもって、訪問ありと判断する

---

## 12. 日別入力ダイアログ

利用者名と施設名以外の入力は、原則としてプルダウン形式とする。

入力項目は、画面上から順に操作すれば登録が完了する配置にする。

### 12.1 訪問看護基本療養費

独立したプルダウンとする。

- 対象
- 対象外

初期値：`対象`

### 12.2 訪問看護管理療養費

訪問看護基本療養費とは別の独立したプルダウンとする。

- 対象
- 対象外

初期値：`対象`

訪問看護基本療養費と訪問看護管理療養費は同時に対象を選択できること。

1つの「訪問の種類」プルダウンへ統合しない。

### 12.3 訪問職種

- 保健師
- 助産師
- 看護師
- 准看護師
- 理学療法士
- 作業療法士
- 言語聴覚士

初期値：`看護師`

### 12.4 1日当たりの訪問回数

- 1回
- 2回
- 3回以上

初期値：`1回`

「3回以上」を選択した場合は、追加プルダウンで3回から10回までの正確な回数を選択する。

訪問回数に応じて、時間入力欄を動的に増減させる。

### 12.5 各回の訪問時間入力

各訪問について次を入力または表示する。

| 項目 | 入力方式 |
|---|---|
| 開始時刻 | プルダウン |
| 終了時刻 | プルダウン |
| 終了日区分 | プルダウン |
| 訪問時間 | 自動表示 |
| 訪問時間帯 | 自動表示 |
| 時間帯別内訳 | 自動表示 |

開始時刻・終了時刻：

- 00:00から23:55
- 5分刻み

終了日区分：

- 当日
- 翌日

初期値：`当日`

### 12.6 訪問時間

開始時刻、終了時刻、終了日区分から分単位で自動計算する。

利用者による直接入力や変更は不可とする。

### 12.7 訪問時間帯

開始時刻、終了時刻、終了日区分から自動判定する。

利用者による直接入力や変更は不可とする。

| 区分 | 時刻 |
|---|---|
| 深夜 | 0:00以上6:00未満 |
| 早朝 | 6:00以上8:00未満 |
| 通常 | 8:00以上18:00未満 |
| 夜間 | 18:00以上22:00未満 |
| 深夜 | 22:00以上24:00未満 |

境界値：

- 6:00は早朝
- 8:00は通常
- 18:00は夜間
- 22:00は深夜

複数の時間帯にまたがる場合は、`時間帯またぎ`と表示し、各時間帯の滞在分数も表示する。

例：

```text
訪問時間：30分
時間帯：時間帯またぎ
内訳：通常15分、夜間15分
```

時間帯またぎの場合は、黄色またはオレンジ色の警告を表示する。

### 12.8 長時間訪問

- 対象外
- 対象

初期値：`対象外`

訪問時間から長時間訪問の可能性を警告してよいが、自動的に対象へ変更しない。

### 12.9 複数名訪問

- 対象外
- 対象

初期値：`対象外`

### 12.10 緊急訪問

- 対象外
- 対象

初期値：`対象外`

### 12.11 特別管理加算

- 対象外
- 特別管理加算Ⅰ
- 特別管理加算Ⅱ

初期値：`対象外`

### 12.12 退院時共同指導加算

- 対象外
- 対象

初期値：`対象外`

### 12.13 退院支援指導加算

- 対象外
- 対象

初期値：`対象外`

---

## 13. 1日複数回訪問

訪問回数が2回以上の場合、回数分の時間入力欄を表示する。

各回の時間帯を個別に自動判定する。

例：

```text
1回目
10:00～10:30
通常
30分

2回目
19:00～19:30
夜間
30分
```

同じ日の訪問時間が重複している場合は保存不可とする。

翌日終了の訪問についても、日付を含めて正しく重複判定する。

---

## 14. 日付セルの表示

登録済みの日付セルには要約のみ表示する。

1日1回の例：

```text
看護師
10:00～10:30
通常
```

加算ありの例：

```text
看護師
10:00～10:30
通常
加算あり
```

複数回の例：

```text
看護師・2回
10:00 / 19:00
通常 / 夜間
加算あり
```

セル内へ全項目を表示しない。

詳細はセルをクリックして確認する。

---

## 15. 日別入力ダイアログのボタン

次のボタンのみを配置する。

- この日の内容を保存
- この日の内容を削除
- 閉じる

削除前は確認ダイアログを表示する。

保存後は、日付を含む分かりやすいメッセージを表示する。

例：

> 6月10日の訪問内容を保存しました。

---

## 16. 画面1の操作ボタン

- 月額費用を計算
- 入力内容をすべてクリア

`月額費用を計算`を主要ボタンとして最も目立たせる。

`入力内容をすべてクリア`は控えめに表示し、実行前に確認する。

計算ボタン押下時に入力チェックを行い、エラーがなければ画面2へ遷移する。

---

# 17. 画面2：費用明細画面

## 17.1 目的

画面1の1か月分の入力から、サービス提供票別表を参考にした形式で費用明細を表示する。

参考画像が存在する場合は、表形式、内訳、合計欄の配置を参考にする。ただし請求帳票を完全再現しない。

## 17.2 ヘッダー表示

- 利用者名
- 施設名
- 対象年月
- 同一建物人数区分
- 自己負担割合

## 17.3 明細テーブル

| 列 | 内容 |
|---|---|
| 区分 | 基本療養費、管理療養費、加算 |
| サービス内容 | 算定項目名 |
| 算定条件 | 職種、人数区分、訪問日数等 |
| 対象日 | 算定対象日 |
| 算定回数 | 回数 |
| 単価 | 円単位 |
| 金額 | 算定回数×単価 |
| 備考 | 警告や補足 |

サービス内容の例：

- 訪問看護基本療養費
- 訪問看護管理療養費
- 夜間・早朝訪問に関する加算
- 深夜訪問に関する加算
- 複数回訪問に関する費用
- 複数名訪問に関する加算
- 長時間訪問に関する加算
- 緊急訪問に関する加算
- 特別管理加算Ⅰ
- 特別管理加算Ⅱ
- 退院時共同指導加算
- 退院支援指導加算

同じ算定条件の明細は可能な範囲でまとめる。

確認が必要な明細は日別に分けて表示してよい。

## 17.4 合計欄

- 訪問看護基本療養費合計
- 訪問看護管理療養費合計
- 各種加算合計
- 月額費用総額
- 利用者負担額の概算

計算式：

```text
月額費用総額
＝訪問看護基本療養費合計
＋訪問看護管理療養費合計
＋各種加算合計
```

```text
利用者負担額概算
＝月額費用総額
×自己負担割合
```

自己負担割合が未設定の場合、利用者負担額は表示しない。

初期実装の端数処理は1円未満四捨五入とし、将来変更可能な構造にする。

## 17.5 操作ボタン

- 月間入力へ戻る
- 再計算する

戻る操作で入力内容を失わないこと。

PDF、CSV、印刷、請求データ出力は実装しない。

---

# 18. 費用計算ロジック

## 18.1 基本方針

金額や算定条件をVueコンポーネントへ直接記述しない。

最終的な計算はElectronメインプロセス側の専用サービスで行う。

想定サービス：

```text
MonthlyEstimateCalculator
DailyVisitCalculator
TimeZoneClassifier
PricingRuleResolver
```

画面表示用の一時計算をレンダラー側で行ってもよいが、画面2の正式な表示値はメインプロセスの計算結果を使用する。

## 18.2 週単位判定

週単位の訪問日数は日曜日から土曜日を1週間として判定する。

対象月の1日が週途中でも、実際の曜日に基づいて処理する。

## 18.3 計算明細型

```ts
type CalculationLine = {
  category: "basic" | "management" | "addition";
  serviceName: string;
  conditionSummary: string;
  targetDates: string[];
  quantity: number;
  unitPrice: number;
  subtotal: number;
  note?: string;
};
```

---

# 19. 料金マスター

料金をソースコードへハードコーディングしない。

SQLiteに料金マスターを持つ。

必要項目：

- ID
- 項目コード
- 項目名
- 区分
- 適用開始日
- 適用終了日
- 訪問職種
- 同一建物人数区分
- 週内訪問日数区分
- 1日の訪問回数区分
- 時間帯区分
- 加算種別
- 単価
- 単位
- 端数処理
- 備考
- 有効・無効
- サンプル料金フラグ

単位：

- 1回当たり
- 1日当たり
- 1月当たり

正式料金が提供されていない項目は推測しない。

正式料金がない場合は、次のいずれかとする。

- サンプル料金として明確に登録する
- 計算対象外とし、警告を表示する

サンプル料金使用時は画面1と画面2に次を表示する。

> 現在はサンプル料金を使用しています。正式な費用計算には料金マスターの更新が必要です。

READMEに料金マスターの更新方法を記載する。

---

# 20. データモデル

必要最小限とする。

## monthly_estimates

- id
- patient_name
- facility_name
- target_month
- same_building_category
- copayment_rate
- created_at
- updated_at

## daily_visits

- id
- monthly_estimate_id
- visit_date
- basic_fee_applicable
- management_fee_applicable
- profession
- visit_count
- long_visit_type
- multiple_staff_type
- emergency_type
- special_management_type
- discharge_joint_guidance_type
- discharge_support_guidance_type
- created_at
- updated_at

## visit_time_slots

- id
- daily_visit_id
- sequence
- start_time
- end_time
- end_day_type
- duration_minutes
- time_zone_type
- time_zone_breakdown_json

## pricing_rules

料金マスターを保持する。

## schema_migrations

DBマイグレーションを管理する。

計算結果は画面表示時に再生成し、初期版では保存しなくてよい。

---

# 21. 入力チェック

## 21.1 保存不可のエラー

- 利用者名が未入力
- 対象年月が未入力
- 訪問開始時刻が未入力
- 訪問終了時刻が未入力
- 訪問時間が0分
- 当日終了で終了時刻が開始時刻以前
- 訪問回数分の時間入力が不足
- 同日の訪問時間が重複
- 必須プルダウンが未選択
- 翌日終了の結果が24時間以上
- 不正な日付や時刻

## 21.2 保存可能だが警告する条件

- 複数の時間帯にまたがる
- 訪問時間が著しく短い
- 長時間訪問に該当する可能性がある
- 深夜から翌日にまたがる
- 加算と入力条件が不整合の可能性がある
- 料金マスターに一致する項目がない
- サンプル料金を使用している

エラーは赤、警告は黄色またはオレンジで表示する。

色だけでなく、アイコンと文章を併用する。

---

# 22. UI・UX要件

利用者はITに不慣れであることを常に前提とする。

- 文字サイズを小さくしない
- ボタンを押しやすい大きさにする
- 主要操作を1つに絞って目立たせる
- 専門用語には短い説明を付ける
- 入力順序を上から下へ統一する
- 必須項目を明示する
- 初期値を適切に設定する
- 1画面に入力欄を詰め込みすぎない
- 日別入力はダイアログで行う
- 保存後に完了メッセージを表示する
- エラーは何を直すべきか文章で示す
- 削除と全消去のみ確認ダイアログを表示する
- 青系または緑系の落ち着いた配色にする
- 赤色を通常表示に使用しない
- 色だけで状態を表現しない
- PC画面を優先する
- 1366×768程度でも利用できる
- キーボード操作を必須にしない
- ダブルクリックを必須にしない
- 横スクロール中も操作位置を見失わせない
- 戻る操作で入力内容を消さない

利用者向け画面には次の技術用語を表示しない。

- IPC
- SQLite
- Renderer
- Migration
- API
- Stack trace

---

# 23. 対象外機能

以下は実装しない。

- レセプト作成
- 保険請求
- 国保連連携
- 支払基金連携
- 電子カルテ連携
- 看護記録
- 看護計画
- 入金管理
- 請求書発行
- 利用者マスター
- 施設マスター
- 職員マスター
- ログイン
- 権限管理
- 操作履歴
- PDF出力
- CSV出力
- 印刷専用機能
- メール通知
- クラウド同期
- 自動アップデート
- 複数PC間のデータ共有
- 同時編集
- 複数利用者の一括計算

---

# 24. プロジェクト構成例

```text
hokan-kantan/
├── src/
│   ├── main/
│   │   ├── database/
│   │   ├── migrations/
│   │   ├── repositories/
│   │   ├── services/
│   │   ├── validators/
│   │   ├── ipc/
│   │   └── main.ts
│   ├── preload/
│   │   ├── index.ts
│   │   └── types.ts
│   └── renderer/
│       ├── components/
│       ├── views/
│       ├── composables/
│       ├── stores/
│       ├── types/
│       ├── router/
│       └── main.ts
├── resources/
│   ├── icons/
│   └── pricing/
├── tests/
│   ├── unit/
│   └── integration/
├── docs/
│   ├── requirements.md
│   ├── design.md
│   └── screen-design.md
├── package.json
├── electron-builder.yml
├── vite.config.ts
├── tsconfig.json
└── README.md
```

過剰なフォルダ分割や抽象化を避ける。

---

# 25. テスト要件

## 25.1 時間帯判定

- 5:59は深夜
- 6:00は早朝
- 7:59は早朝
- 8:00は通常
- 17:59は通常
- 18:00は夜間
- 21:59は夜間
- 22:00は深夜
- 23:30から翌0:30は深夜
- 17:45から18:15は時間帯またぎ
- 7:45から8:15は時間帯またぎ
- 21:45から22:15は時間帯またぎ

## 25.2 訪問時間計算

- 当日10:00から10:30は30分
- 当日終了で終了時刻が開始時刻以前の場合はエラー
- 23:30から翌0:10は40分
- 開始時刻と終了時刻が同じ場合はエラー
- 24時間以上になる入力はエラー

## 25.3 複数回訪問

- 1日2回の時間が重複しない場合は保存できる
- 1日2回の時間が重複する場合は保存できない
- 1回目が翌日にまたがる場合も重複を判定できる

## 25.4 月間計算

- 基本療養費のみ
- 基本療養費と管理療養費
- 夜間訪問を含む
- 深夜訪問を含む
- 1日2回訪問を含む
- 特別管理加算を含む
- 退院時共同指導加算を含む
- 退院支援指導加算を含む
- 自己負担割合未設定
- 自己負担割合1割
- 月をまたぐ週の週内訪問日数
- 料金マスターに該当項目がない
- サンプル料金を使用している

## 25.5 データ保持

- 初回起動時にDBが自動作成される
- アプリ再起動後も入力が保持される
- アプリ更新後もDBが保持される
- マイグレーションを複数回実行しても重複しない

---

# 26. README要件

## 開発者向け

- 必要なNode.jsバージョン
- パッケージインストール方法
- 開発環境の起動方法
- 型チェック、Lint、テストの実行方法
- Windowsインストーラー作成方法
- SQLiteファイルの保存場所
- DBマイグレーション方法
- 料金マスター更新方法
- サンプル料金と正式料金の区別
- アプリバージョン変更方法

## 利用者向け

専門用語を避け、次を記載する。

- インストール方法
- 起動方法
- 月額費用の計算方法
- 入力内容の削除方法
- アプリの終了方法
- データ保存場所
- アンインストール後もデータが残る場合があること
- 計算結果は概算であること

---

# 27. 開発の進め方

次の順番で進める。

1. 既存リポジトリを確認する
2. 既存構成と本要件の差分を整理する
3. `docs/requirements.md`を作成する
4. `docs/design.md`を作成する
5. `docs/screen-design.md`を作成する
6. Electron、Vue、Vuetify、TypeScriptの基本構成を作成する
7. SQLite初期化とマイグレーションを実装する
8. Electron IPCを実装する
9. 月間入力画面を実装する
10. 日別入力ダイアログを実装する
11. 時間帯判定を実装する
12. 複数回訪問を実装する
13. 料金マスターを実装する
14. 費用計算サービスを実装する
15. 費用明細画面を実装する
16. 入力チェックと警告を実装する
17. テストを実装する
18. サンプル料金データを登録する
19. Windowsインストーラーを作成する
20. READMEを作成する

一度に全機能を変更せず、機能単位で実装する。

各段階で型チェック、Lint、テストを実行する。

---

# 28. MVP完了条件

次をすべて満たした時点をMVP完成とする。

1. Windowsへインストールできる
2. Node.jsやSQLiteを別途導入せず起動できる
3. 初回起動時にDBが自動作成される
4. 月間入力画面が表示される
5. 利用者名、施設名、対象年月を入力できる
6. 日付を選択して日別訪問内容を登録できる
7. 対象・加算項目をプルダウンで操作できる
8. 開始・終了時刻をプルダウンで入力できる
9. 訪問時間が自動計算される
10. 訪問時間帯が自動判定される
11. 複数回訪問を登録できる
12. 退院時共同指導加算を選択できる
13. 退院支援指導加算を選択できる
14. 1か月分の訪問内容を一覧で確認できる
15. 費用明細画面に明細が表示される
16. 基本療養費、管理療養費、加算の小計が表示される
17. 月額費用総額が表示される
18. 自己負担割合に応じた概算額が表示される
19. 再起動後も入力内容が保持される
20. 料金マスターをコード変更なしで更新できる
21. 自動テストが成功する
22. Windowsインストーラーを生成できる
23. READMEの手順で開発・配布できる

---

# 29. 実装判断の優先順位

要件に明記されていない内容は、次の順に優先する。

1. ITに不慣れな利用者が迷わない
2. 入力ミスを防止できる
3. 計算根拠を確認できる
4. 保守しやすい
5. 実装が単純である

要件にない機能を独自に追加しない。

正式な算定条件や金額が不明な場合は推測しない。

不明点があっても開発を全面停止せず、影響のない部分から進める。

---

# 30. Codexの作業ルール

- 作業開始前にリポジトリ構成を確認する
- 変更前に実装方針を簡潔に示す
- 大規模変更は小さな単位へ分ける
- 既存コードを不必要に全面書き換えしない
- 料金計算、DB、IPC、UIの責務を分離する
- 型安全を維持する
- エラーを握りつぶさない
- 利用者向けエラーには技術情報を表示しない
- 変更後に型チェック、Lint、テストを実行する
- 実行できなかった確認項目は、理由とともに報告する
- 要件と異なる判断をした場合は、理由を報告する

---

# 31. Codexの最終報告

実装完了後、次を報告する。

- 実装した機能
- 作成・変更した主なファイル
- 画面構成
- データモデル
- IPCの構成
- 費用計算の仕組み
- 時間帯判定の仕組み
- 使用料金が正式料金かサンプル料金か
- 開発環境の起動方法
- テストの実行方法
- インストーラーの作成方法
- SQLiteファイルの保存場所
- 未実装事項
- 確認が必要な算定条件
- 実際に実行したコマンドと結果
